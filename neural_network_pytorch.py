import torch
import torch.nn as nn
import torch.optim as optim
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler

# Define neural network
# TODO: Create a simple feedforward neural network class

# Load and preprocess data
# TODO: Load dataset, split into training/testing, and standardize features

# Train model
# TODO: Train the neural network and plot the loss curve